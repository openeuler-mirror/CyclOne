import { ioDateFormat } from '../../../helpers/io-date-format';
import { module, test } from 'qunit';

module('Unit | Helper | io date format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ioDateFormat(42);
  assert.ok(result);
});
