import { ioMoment } from '../../../helpers/io-moment';
import { module, test } from 'qunit';

module('Unit | Helper | io moment');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ioMoment(42);
  assert.ok(result);
});
