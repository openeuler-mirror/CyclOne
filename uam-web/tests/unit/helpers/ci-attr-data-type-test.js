import { ciAttrDataType } from '../../../helpers/ci-attr-data-type';
import { module, test } from 'qunit';

module('Unit | Helper | ci attr data type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ciAttrDataType(42);
  assert.ok(result);
});
