import { booleanDataType } from '../../../helpers/boolean-data-type';
import { module, test } from 'qunit';

module('Unit | Helper | boolean data type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = booleanDataType(42);
  assert.ok(result);
});
